on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Sync files via FTP
        uses: sebastianpopp/ftp-action@v1.0.1  # Use an available version
        with:
          host: ftpupload.net
          user: ezyro_38543176
          password: ${{ secrets.FTP_PASSWORD }}
          localDir: "./"
          remoteDir: "/public_html/"